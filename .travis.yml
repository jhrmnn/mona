language: python
matrix:
  include:
    - python: 3.7
      env: TOXENV=py37-coverage-codecov
      dist: xenial
      sudo: required
    - python: 3.6
      stage: deploy
      env: TOXENV=docs
before_install:
  - pip install --upgrade pip"<=18.0" tox tox-venv
  - curl -sSL https://raw.githubusercontent.com/sdispater/poetry/master/get-poetry.py | python
install: true
script: tox
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  local-dir: docs/build
  on:
    branch: master
    condition: $TOXENV = docs
